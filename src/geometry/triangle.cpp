#include "triangle.h"

namespace raytracer {
namespace geometry {

bool Triangle::hit(const core::Ray& ray, float t_min, float t_max, HitRecord& rec) const {
    // TODO: Implement triangle intersection (Möller-Trumbore)
    return false;
}

} // namespace geometry
} // namespace raytracer
